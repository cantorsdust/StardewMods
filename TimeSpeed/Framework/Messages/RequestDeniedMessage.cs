namespace TimeSpeed.Framework.Messages;

/// <summary>A multiplayer message from the host indicating that a request to change time was rejected, because the host isn't configured to allow it.</summary>
internal class RequestDeniedMessage;
